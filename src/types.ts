
export interface Music {
  id: string;
  title: string;
  artist: string;
  coverUrl: string;
  audioUrl: string;
  duration: string;
  uploadDate: string;
}

export interface Photo {
  id: string;
  title: string;
  imageUrl: string;
  description: string;
  uploadDate: string;
}
